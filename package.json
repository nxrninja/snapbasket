{
  "name": "snapbasket",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm install --prefix backend && npm install --prefix backend/frontend && npm run build --prefix backend/frontend",
    "start": "npm run start --prefix backend",
    "dev": "npm run dev --prefix backend"
  }
}

